# Data

No external dataset is used. The only corpus is the local operational runbook in `rag/runbook.md`, duplicated in `docs/runbook.md` for review. Runtime incident records are generated by local demos and stored in the FMG SQLite database.
